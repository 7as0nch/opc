# 业务接口基地址（客户端）。默认走同源代理 /api/backend（推荐，规避跨域 CORS/混合内容）。一般留空。
NEXT_PUBLIC_API_BASE_URL=
# 接口模式：backend（真实，默认）| mock（本地无后端时返回模拟数据）
NEXT_PUBLIC_API_MODE=
# 业务后端真实地址前缀（服务端，仅 /api/backend 代理使用；与埋点同一后端）
API_UPSTREAM_URL=https://api.aihelper.chat

# 站点对外地址，用于 SEO canonical / sitemap / OpenGraph
NEXT_PUBLIC_SITE_URL=https://chengj.online

# 首页数据统计（项目数/累计活跃度）是否展示。默认隐藏；数据量上来后置为 true 再展示。
# 注意：NEXT_PUBLIC_* 为构建期内联，改值需重新构建（publish 时加 --build-arg）。
NEXT_PUBLIC_SHOW_STATS=

# 埋点上报地址（客户端）。默认走同源代理 /api/tracker/batch（推荐，规避跨域/混合内容）。
# 一般留空即可；仅在你想让浏览器直连某个支持 CORS 的 https 接口时才填。
NEXT_PUBLIC_TRACKER_URL=
# 埋点应用标识
NEXT_PUBLIC_TRACKER_APP_ID=opc-web

# 埋点真实后端地址（服务端，仅 /api/tracker/batch 代理使用，不暴露给浏览器）
TRACKER_UPSTREAM_URL=https://api.aihelper.chat/tracker/batch
